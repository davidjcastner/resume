{
  "name": "resume",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite src",
    "dev": "vite src",
    "build": "vite build",
    "pdf": "node scripts/generate-pdf.js",
    "build:all": "vite build && node scripts/generate-pdf.js"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "puppeteer": "^24.0.0"
  }
}
